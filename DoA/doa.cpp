#include "doa.hpp"
#include <cmath>
#include <algorithm>

vector<double> music_doa(const MatrixXcd& covariance_matrix, int num_sources, const vector<double>& angles) {
    // Placeholder for MUSIC algorithm
    return vector<double>(num_sources, 0.0);
}
